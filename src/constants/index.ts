export const ERROR_RESPONSE = {
  TOKEN_NOT_FOUND: { error: "Token not found" },
  INVALID_TOKEN: { error: "Invalid authorization header" },

  RECORD_NOT_FOUND: { error: "Record not found" },

  SERVER_ERROR: { error: "Server error" },
};

export const verbosityLevelMapping = [24, 48, 64, 72, 96, 128];
